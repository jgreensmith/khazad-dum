# Experiment Infrastructure Summary

The data below describes the live experiment VMs khazad-dum just provisioned on AWS.

Produce a short operator briefing:
- One line per server: name, region, public IP.
- The exact SSH command to connect to each server.
- Note the login user is `ubuntu` and each experiment has its own private key.

Be concise. Use only the data provided; do not invent hosts, IPs, or credentials.
