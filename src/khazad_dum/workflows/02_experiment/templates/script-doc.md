# Payload `{{name}}` — {{role}} ({{target}})

<!-- One per payload dir; lives beside `run`. The report stage appends it as an
     appendix, so the report fully documents what was run. Keep it short + factual. -->

## What it does
<!-- This endpoint's part in the benchmark, in 2–4 sentences. -->

## How it runs
<!-- The app port it uses; how it branches on $KHAZAD_ROLE and reaches the peer at
     $KHAZAD_PEER_ADDR; how the server side terminates; what provision.sh installs. -->

## Results written
<!-- Each file written to $KHAZAD_RESULTS_DIR: name, format, columns/fields, units. -->
