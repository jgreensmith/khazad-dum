# Step 06 — Delivery and Maintenance (Claude)

**Phase 2 (Active Build), Step 2.2.** Final step: deliver the built system and maintain it.

## Status
**Not authored.** `prompts/prompt.md` + `templates/template.md` are empty placeholders. Deliverables
are **undefined** and need an interview with James before authoring. Intended as a single prompt (not
a loop). See `roadmap.md`.

## Intended shape (placeholder)
- Phase 2, so it shares the global prompt `workflows/active-build-phase.md` (tier-agnostic, no git for
  agents, scope.md is the complete context).
- Runs after Developer Orchestration (05) has built the features.

## Key files
`workflows/06_delivery_and_maintenance/prompts/prompt.md` (empty),
`templates/template.md` (3-line stub).
